import { React, Fragment } from 'react'

function Workshops({ id }) {
	return (
		<Fragment>
			<div id={id} className='bg-light position-relative z-index-0'>
				Simple workshops div
			</div>
		</Fragment>
	)
}

export default Workshops
